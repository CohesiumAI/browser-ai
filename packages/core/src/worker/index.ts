/**
 * Worker entry point.
 * Placeholder for V0.1 — worker logic will be integrated with providers.
 */

export {
  createEnvelopeFactory,
  isCurrentEpoch,
  type Envelope,
  type WorkerCommand,
  type WorkerEvent,
} from '../index.js';
