{
  "name": "@browser-ai/providers-webnn",
  "version": "1.0.0",
  "description": "WebNN provider for browser-ai — hardware-accelerated inference",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@browser-ai/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.2.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  },
  "peerDependencies": {
    "@browser-ai/core": ">=0.2.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT"
}
