{
  "name": "browser-ai",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @browser-ai/example-vite-text dev",
    "dev:next": "pnpm --filter @browser-ai/example-next dev",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm -r --filter \"./packages/**\" build && changeset publish",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter @browser-ai/example-vite-text test:e2e",
    "verify": "pnpm lint && pnpm build && pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.10",
    "@typescript-eslint/eslint-plugin": "^8.8.0",
    "@typescript-eslint/parser": "^8.8.0",
    "eslint": "^9.10.0",
    "tsup": "^8.2.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.1"
  }
}
